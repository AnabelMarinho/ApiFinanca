# Configuração temporária para Railway
# TODO: Mover essas configurações para variáveis de ambiente no Railway após configurar GitHub Secrets

spring.application.name=ApiFinanca

# Database configuration - Railway PostgreSQL
spring.datasource.url=jdbc:postgresql://interchange.proxy.rlwy.net:43797/railway
spring.datasource.username=postgres
spring.datasource.password=tzsThDaphtefbXZCUoNmqCpwBWwTYgDy

# Server port - Railway provides PORT environment variable
server.port=${PORT:8080}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JWT configuration - uses environment variables with fallback
app.jwt.secret=${JWT_SECRET:change-this-secret-key-to-a-secure-value}
app.jwt.expiration=${JWT_EXPIRATION:3600000}

# Springdoc OpenAPI configuration
springdoc.override-with-generic-response=false

